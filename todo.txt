- support UTF-16
    - Use BOM if applicable to determine file encoding (and, of course, byte order)

- use a slab allocator for all rope_t allocations
    - or, maybe find a way to use something like jon blow's 'temporary allocator' (keep a temp buffer in a context for example)
